{
  "name": "kannedit",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run setup:server",
    "setup:app": "npm run setup:client && npm run setup:server",
    "setup:client": "cd kannedit.client && npm i",
    "setup:server": "cd kannedit.server && npm i",
    "start": "node kannedit.server/index.js"
  },
  "engines": {
    "node": "^14.17.0"
  }
}
